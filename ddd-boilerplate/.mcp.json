{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$comment": "Claude Code MCP Configuration - filesystem サーバーは意図的に除外（コンテキストオーバーフロー防止）",
  "mcpServers": {
    "memory": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"],
      "description": "Persistent memory for storing domain context and ubiquitous language"
    },
    "sequential-thinking": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"],
      "description": "Step-by-step reasoning for complex architectural decisions"
    },
    "claude-flow": {
      "command": "npx",
      "args": ["claude-flow@alpha", "mcp", "start"],
      "description": "Claude Flow MCP server for swarm, memory, and architecture validation"
    },
    "taskmaster-ai": {
      "command": "npx",
      "args": ["-y", "task-master-ai"],
      "env": {
        "TASK_MASTER_TOOLS": "standard",
        "ANTHROPIC_API_KEY": "${ANTHROPIC_API_KEY}",
        "PERPLEXITY_API_KEY": "${PERPLEXITY_API_KEY}",
        "OPENAI_API_KEY": "${OPENAI_API_KEY}",
        "TASK_MASTER_RESEARCH_MODEL": "perplexity/sonar-pro"
      },
      "description": "AI-powered task management for DDD/Clean Architecture development with research capabilities"
    }
  },
  "_disabled_servers": {
    "_comment": "以下のサーバーは意図的に無効化しています。有効にする場合は mcpServers に移動してください。",
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "./src", "./tests", "./docs"],
      "description": "⚠️ 警告: directory_tree コマンドは node_modules を含む巨大なレスポンスを返す可能性があります。Claude Code の組み込みツール（Read, Write, LS, Glob）を使用することを推奨します。"
    }
  }
}
